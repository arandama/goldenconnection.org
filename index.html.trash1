<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Golden Connection - Technology Help for Seniors</title>
    <meta name="description" content="Technology education for seniors 55+ in smaller communities. Patient, friendly instruction for smartphones, computers, and staying connected with family.">
    <style>
        /* --------------------------------------------------------------
           Existing stylesheet (unchanged) – only a few additions are marked
           with comments “/* PHASE‑1 ADDITION */”.
        ------------------------------------------------ */
        * {margin:0;padding:0;box-sizing:border-box;}
       body{font-family:'Segoe UI',Tahoma,Geneva,Verdana,sans-serif;line-height:1.8;color:#222;font-size:18px;scroll-behavior:smooth;}
        .container{max-width:1200px;margin:0 auto;padding:0 20px;}

        /* … (all the original CSS you already have) … */

        /* --------------------------------------------------------------
    PHASE‑1 – COOKIE CONSENT BANNER STYLES
        -------------------------------------------- */
        #cookieBanner{
            position:fixed;
            bottom:0;
            left:0;
            right:0;
            background:#fff;
            border-top:2px solid #1565c0;
          padding:1rem 2rem;
            box-shadow:0 -4px 12px rgba(0,0,0,.1);
       font-size:14px;
            z-index:9999;
            display:flex;
            flex-wrap:wrap;
            align-items:center;
            justify-content:space-between;
        }
        #cookieBanner p{margin:0 0 .5rem 0;flex:1 1 300px;}
        #cookieBanner button{
            background:#1565c0;
            color:#fff;
            border:none;
            padding:.5rem 1rem;
            margin:.25rem;
            border-radius:4px;
            cursor:pointer;
            font-size:14px;
        }
        #cookieBanner buton:hover{background:#1976d2;}
        #cookieBanner .btn-group{flex:0 0 auto;}
        @media (prefers-reduced-motion:reduce){
            #cookieBanner{transition:none;}
        }
    </style>
</head>
<body>
    <!-- Skip Navigation -->
    <a href="#main-content" class="skip-nav">Skip to main content</a>

    <!-- Font Size Controls -->
    <div class="font-controls">
        <label for="font-size">Text Size:</label>
        <button onclick="changeFontSize('small')" aria-label="Small text size">A</button>
        <button onclick="changeFontSize('medium')" aria-label="Medium text size">A</button>
        <button onclick="changeFontSize('large')" aria-label="Large text size">A</button>
    </div>

    <!-- Header -->
    <header class="header">
        <nav class="nav container" role="navigation" aria-label="Main navigation">
            <a href="#" class="logo">Golden Connection</a>
            
         <div class="header-contact">
                <a href="tel:+15551238324" class="header-phone" aria-label="Call us now">
                    📞 (555) 123-TECH
            </a>
                <a href="#easter-eggs" class="easter-egg-link">
                    🥚 Discover Blu's Secrets
                </a>
            </div>

            <ul class="nav-links">
                <li><a href="#services">Services</a></li>
                <li><a href="#how-it-works">How It Works</a></li>
                <li><a href="#topics">What We Teach</a></li>
                <li><a href="#venue-contact">Venues</a></li>
                <li><a href="#contact">Get Started</a></li>
            </ul>
            <a href="#contact" class="cta-button">Get Help Today</a>
            <button class="mobile-menu-btn" onclick="toggleMobileMenu()" aria-expanded="false" aria-controls="mobileNav" aria-label="Open menu">☰</button>
            <nav class="mobile-nav" id="mobileNav" role="navigation" aria-label="Mobile navigation">
                <ul>
                    <li><a href="#services" onclick="closeMobileMenu()">Services</a></li>
                    <li><a href="#how-it-works" onclick="closeMobileMenu()">How It Works</a></li>
                    <li><a href="#topics" onclick="closeMobileMenu()">What We Teach</a></li>
                    <li><a href="#venue-contact" onclick="closeMobileMenu()">Venues</a></li>
                    <li><a href="#contact" onclick="closeMobileMenu()">Get Started</a></li>
                </ul>
            </nav>
        </nav>
    </header>

    <!-- Main Content -->
    <main id="main-content">
        <!-- (All the existing sections – unchanged) -->
        …
    </main>

    <!-- Footer -->
    <footer class="footer" role="contentinfo">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h4>Golden Connection</h4>
                    <p>Bridging the digital divide for seniors in communities across America. A nonprofit organization dedicated to patient, accessible technology education.</p>
                </div>
           <div class="footer-section">
                    <h4>Our Services</h4>
                    <a href="#services">Group Sessions</a>
                    <a href="#services">One-on-One Support</a>
                    <a href="#topics">What We Teach</a>
                    <a href="#venue-contact">Partner Venues</a>
                </div>
                <div class="footer-section">
                    <h4>Contact Information</h4>
             <a href="tel:+15551238324">(555) 123-TECH</a>
                    <a href="mailto:hello@goldenconnection.org">hello@goldenconnection.org</a>
                </div>
                <div class="footer-section">
                    <h4>Legal &amp; Policies</h4>
                    <a href="privacy-policy.html">Privacy Policy</a>
                    <a href="cookie-policy.html">Cookie Policy</a>
                    <a href="privacy-policy.html#ccpa">Do Not Sell My Info</a>
                </div>
            </div>
            <div style="border-top:1px solid #444;padding-top:20px;margin-top:20px;">
                <p>&copy; 2025 Golden Connection. A nonprofit organization dedicated to senior technology education.</p>
                <p style="margin-top:10px;font-size:1em;">
                    <a href="#" style="color:#4caf50;text-decoration:none;">Privacy Policy</a> |
                    <a href="cookie-policy.html" style="color:#4caf50;text-decoration:none;">Cookie Policy</a> |
                    <a href="javascript:window.print()" style="color:#4caf50;text-decoration:none;">Print This Page</a>
                </p>
            </div>
        </div>
    </footer>

    <!-- Back to Top Button -->
    <button class="back-to-top" onclick="scrollToTop()" aria-label="Back to top">↑</button>

    <!-- --------------------------------------------------------------
         PHASE‑1 – COOKIE CONSENT BANNER MARKUP
    -------------------------------------------------------------- -->
    <div id="cookieBanner" role="dialog" aria-live="polite" aria-label="Cookie consent banner">
        <p>
            We use cookies to improve your experience. <strong>Essential</strong> cookies are always active. You may choose to allow <strong>Analytics</strong> and <strong>Functional (Maps)</strong> cookies, or reject them.
        </p>
        <div class="btn-group">
            <button id="acceptAll">Accept All</button>
            <button id="rejectAll">Reject All</button>
            <button id="customize">Customize…</button>
        </div>
    </div>

    <!-- --------------------------------------------------------------
         Existing Scripts (unchanged) – placed AFTER the banner so the banner
         can block any future non‑essential scripts.
    -------------------------------------------------------------- -->
    <script>
        // --------------------------------------------------------------
        // PHASE‑1 – COOKIE CONSENT LOGIC
        // --------------------------------------------------------------
        (function(){
            const banner   = document.getElementById('cookieBanner');
            const accept   = document.getElementById('acceptAll');
            const reject   = document.getElementById('rejectAll');
            const custom   = document.getElementById('customize');

            // Helper – write a simple first‑party cookie (expires in 365 days)
            function setCookie(name,value,days){
                const d = new Date();
                d.setTime(d.getTime() + (days*24*60*60*1000));
                document.cookie = `${name}=${value};expires=${d.toUTCString()};path=/;SameSite=Lax`;
            }
            function getCookie(name){
                const match = document.cookie.match(new RegExp('(^| )'+name+'=([^;]+)'));
                return match ? match[2] : null;
            }

            // Determine if we already have a consent record
            const existing = getCookie('cookie_consent');
            if(existing){
                // Consent already given – hide banner
                banner.style.display='none';
                // If analytics were allowed, you could now load them:
                // if(existing.includes('analytics')) loadAnalytics();
                return;
            }

            // ----------------------------------------------------------
            // Button handlers
            // ----------------------------------------------------------
            accept.addEventListener('click',()=> {
                // essential + analytics + functional
                setCookie('cookie_consent','essential,analytics,functional',365);
                banner.style.display='none';
                // loadAnalytics(); // <-- uncomment when you add analytics
            });
            reject.addEventListener('click',()=> {
                // only essential
                setCookie('cookie_consent','essential',365);
                banner.style.display='none';
            });
            custom.addEventListener('click',()=> {
                // Very simple custom UI – you can replace with a modal if you wish
                const allowAnalytics = confirm('Allow analytics cookies?');
                const allowFunctional = confirm('Allow functional (maps) cookies?');
                let value = 'essential';
                if(allowAnalytics) value += ',analytics';
                if(allowFunctional) value += ',functional';
                setCookie('cookie_consent',value,365);
                banner.style.display='none';
                // if(allowAnalytics) loadAnalytics();
            });

            // ----------------------------------------------------------
            // Respect Do‑Not‑Track header – if the browser sends it, treat as “reject all”
            // ----------------------------------------------------------
            if (navigator.doNotTrack == "1" || window.doNotTrack == "1") {
                setCookie('cookie_consent','essential',365);
                banner.style.display='none';
            }
        })();

        // --------------------------------------------------------------
        // Existing UI helpers (mobile menu, font size, back‑to‑top, etc.)
        // --------------------------------------------------------------
        function toggleMobileMenu(){ /* unchanged */ }
        function closeMobileMenu(){ /* unchanged */ }
        function changeFontSize(size){ /* unchanged */ }
        function scrollToTop(){ /* unchanged */ }

        // (All the other functions you already had remain exactly the same.)
    </script>
</body>
</html>
